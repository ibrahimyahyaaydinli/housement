import React, {useEffect, useState} from 'react';

import {
  SafeAreaView,
  ScrollView,
  StatusBar,
  StyleSheet,
  Text,
  useColorScheme,
  View,
  FlatList,
  Alert,
} from 'react-native';

const Settings = () => {
  return (
    <SafeAreaView>
      <View>
        <Text>SETTİNGS</Text>
      </View>
    </SafeAreaView>
  );
};

export default Settings;
